# Define upstream servers for different microservices
# These will be replaced with actual service names when deployed

upstream auth_service {
    server auth-service:3000;
}

# Commented out until these services are implemented
# upstream courses_service {
#     server courses-service:3001;
# }

# upstream user_service {
#     server user-service:3002;
# }

# Main server configuration
server {
    listen 80;
    server_name localhost;

    # API Gateway routes
    
    # Auth Service routes
    location /api/auth/ {
        proxy_pass http://auth_service/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Courses Service routes - commented out until implemented
    # location /api/courses/ {
    #     proxy_pass http://courses_service/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
    
    # User Service routes - commented out until implemented
    # location /api/users/ {
    #     proxy_pass http://user_service/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
    
    # Default route
    location / {
        return 200 "LearnExpert API Gateway";
    }
} 